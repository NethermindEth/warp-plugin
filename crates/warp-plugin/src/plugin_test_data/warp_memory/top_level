//! > Test expansion of a function signature depending on warp memory.

//! > test_runner_name
ExpandContractTestRunner

//! > cairo_code
#[implicit(warp_memory)]
fn read_from_memory(index: felt252) {
    warp_memory.read_u128(index);
}

#[implicit(warp_memory)]
fn read_from_memory_2(index: felt252) {
    warp_memory.read_u128(index);
}

//! > expected_diagnostics

//! > generated_cairo_code
use warplib::memory::WarpMemoryTrait;

fn read_from_memory(ref warp_memory: DictFelt252To<u128>, index: felt252) {
    warp_memory.read_u128(index);
}

fn read_from_memory_2(ref warp_memory: DictFelt252To<u128>, index: felt252) {
    warp_memory.read_u128(index);
}
